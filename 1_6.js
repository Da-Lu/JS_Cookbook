//Checking for an Existing, Noneempty String
//true if variable exists, is a string, and has a length greater than zero
if((typeof unknowVariable !="undefined")&&(typeof unknownVariable.valueOf() == "string")&&(unknownVariable.length > 0)){
	//...
}